{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Current File with args",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "args": ["--configs", "debug.json", "--defaultGeometry"]
        }
    ]
}